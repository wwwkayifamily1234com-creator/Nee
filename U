n=int(input())
k=list(map(int,input().split()))
a=0
for i in range(len(k)):
    if k[i]==-1:
        a+=1
m=len(k)-a
b=0
for j in range(len(k)): 
    if k[j] !=-1:
        b+=k[j]   
c=float(b/m)   
print(c)        
